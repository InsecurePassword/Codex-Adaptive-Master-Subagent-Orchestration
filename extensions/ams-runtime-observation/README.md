# AMS runtime-observation companion

Optional AMS 4.0 companion for bounded local rollout-metadata corroboration. It is separate from core and never activates implicitly.

Use only through the active compatible AMS core `adaptive-master-subagent-orchestration/references/runtime-observation.md` gate. The core must resolve the installed companion root from this companion's active `SKILL.md` and execute a verified absolute helper path beneath that root; project-relative `tools/` paths are forbidden.
