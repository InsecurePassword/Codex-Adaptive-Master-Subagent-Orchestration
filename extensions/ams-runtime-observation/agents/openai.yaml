interface:
  display_name: "AMS Runtime Observation"
  short_description: "Optional bounded local routing-metadata corroboration for AMS 4.0"
  default_prompt: "Use only when active AMS 4.0 routes required local runtime observation."
policy:
  allow_implicit_invocation: false
