# AMS app-task lane companion

Optional AMS 4.0 companion for explicit user-visible Codex app tasks. It is separate from core and never activates implicitly.

Use only through the active compatible AMS core route in `adaptive-master-subagent-orchestration/references/runtime-core.md`. Installing the companion does not enable `app_task_lane` or any other optional feature.
