interface:
  display_name: "Adaptive Master–Subagent Orchestration"
  short_description: "Adaptive Sol Max orchestration with intensity controls"
  default_prompt: "Apply adaptive master-subagent orchestration in auto mode unless another intensity is explicitly selected."
policy:
  allow_implicit_invocation: true
