interface:
  display_name: "AMS Installer"
  short_description: "Install and repair orchestration profiles"
  default_prompt: "Install or verify the managed profiles without changing the project."
policy:
  allow_implicit_invocation: false
